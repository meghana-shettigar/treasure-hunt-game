{
  "name": "letter-left-behind-stripe-api",
  "version": "1.0.0",
  "description": "Creates Stripe PaymentIntents for the booking page (keep STRIPE_SECRET_KEY on the server only).",
  "main": "stripe-server.js",
  "scripts": {
    "start": "node stripe-server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "stripe": "^17.0.0"
  }
}

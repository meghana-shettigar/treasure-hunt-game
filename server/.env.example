# Copy to .env locally (do not commit .env)
# Stripe Dashboard → Developers → API keys → Secret key
STRIPE_SECRET_KEY=sk_test_xxxxxxxx

# Production: comma-separated origins (apex + www) for https://letterleftbehind.com booking page.
# Leave unset to allow any Origin (good for local dev with payment server on :3001).
# CLIENT_ORIGIN=https://letterleftbehind.com,https://www.letterleftbehind.com
#
# Local-only (commented):
# CLIENT_ORIGIN=http://localhost:8000

PORT=3001

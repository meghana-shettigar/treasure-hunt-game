# Copy to .env locally (do not commit .env)
# Stripe Dashboard → Developers → API keys → Secret key
STRIPE_SECRET_KEY=sk_test_xxxxxxxx

# Optional: restrict CORS to your live booking page (e.g. https://letterleftbehind.com)
# Leave unset OR use the old placeholder to allow localhost during development.
# CLIENT_ORIGIN=https://your-real-domain.com

PORT=3001
